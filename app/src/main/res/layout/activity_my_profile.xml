<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.contraslash.android.openspeechcorpus.apps.profile.activities.MyProfile"
    android:orientation="vertical"

    >
    <android.support.v7.widget.Toolbar
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/my_profile_toolbar"
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:minHeight="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        app:theme="@style/ThemeOverlay.AppCompat.Light"
        />
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical"
        android:layout_margin="@dimen/margin_lateral_default">
        <ImageView
            android:layout_width="@dimen/tam_profile_image"
            android:layout_height="@dimen/tam_profile_image"
            android:id="@+id/my_profile_profile_image"
            android:src="@drawable/ic_account_box_18pt_3x"/>

        <EditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/margin_lateral_default"
            android:id="@+id/my_profile_profile_name"
            android:hint="@string/nick"
            android:maxLines="1"
            android:singleLine="true"/>

        <ImageButton
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_save_18pt_3x"
            style="?android:buttonBarButtonStyle"
            android:layout_marginTop="@dimen/margin_lateral_default"
            android:contentDescription="@string/save"
            android:id="@+id/my_profile_save"/>

    </LinearLayout>

</LinearLayout>
